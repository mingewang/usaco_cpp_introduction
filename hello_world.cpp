#include <iostream> // Include the input-output stream library

int main() {
    std::cout << "Hello, USACO World!" << std::endl; // Print message to the console
    return 0; // Indicate successful execution
}